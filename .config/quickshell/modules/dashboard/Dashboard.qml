import QtQuick
import QtQuick.Layouts

Item {
  ColumnLayout {
    anchors.fill: parent
    Text { text: "Dashboard"; color: "#111"; font.pixelSize: 18; font.bold: true }
    // acá metés sys usage, shortcuts, etc.
  }
}